name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: test-1
        run: echo "HOGE=${{ env.NAME }}" >> ${{ github.env }}
        env:
          NAME: 'matazou'
      - name: test-2
        run: |
          echo $HOGE
          echo ${{ env.HOGE }}
  Other:
    runs-on: ubuntu-latest
    needs: Explore-GitHub-Actions
    steps:
      - name: test-3
        run: |
          echo $HOGE
          echo ${{ env.HOGE }}
